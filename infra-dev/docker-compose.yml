version: '3.8'

services:

  db:
    image: postgres:latest
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    ports:
      - 5432:5432
    env_file:
      - ./.env

  # backend:
  #   image: oskalov/foodgram_backend
  #   restart: always
  #   volumes:
  #     - static_value:/app/static_backend/
  #     - media_value:/media/
  #   depends_on:
  #     - db
  #   links:
  #     - db:db
  #   env_file:
  #     - ./.env

  # frontend:
  #   image: oskalov/foodgram_frontend
  #   volumes:
  #     - ../frontend/:/app/result_build/
  #   depends_on:
  #     - backend

  # backend:
  #   build:
  #     context: ../backend
  #     dockerfile: Dockerfile
  #   restart: always
  #   volumes:
  #     - static_value:/app/static/
  #     - media_value:/app/static/
  #   depends_on:
  #     - db
  #   env_file:
  #     - ./.env
  
  frontend:
    build:
      context: ../frontend
      dockerfile: Dockerfile
    volumes:
      - ../frontend/:/app/result_build/

  nginx:
    image: nginx:1.19.3
    ports:
      - "80:80"
    # extra_hosts:
    #   - "host.docker.internal:host-gateway"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - ../frontend/build:/usr/share/nginx/html/
      - ../docs/:/usr/share/nginx/html/api/docs/
      # - static_value:/var/html/static/
      # - media_value:/var/html/static/
    # depends_on:
    #   - backend

volumes:
  postgres_data:
  # static_value:
  # media_value: